import React from 'react';
import SectionMission from './index';
import mock from './_mock';

export default { title: 'Section Mission Component' };

export const sectionMissionComponent = () => <SectionMission {...mock.props} />;
